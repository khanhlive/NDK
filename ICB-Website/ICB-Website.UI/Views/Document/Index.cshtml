
@{
    ViewBag.Title = "Tài liệu - Văn bản";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
    List<ICB_Website.UI.Models.Breadcrumb> list = new List<ICB_Website.UI.Models.Breadcrumb>();
    list.Add(new ICB_Website.UI.Models.Breadcrumb { ID = 1, Name = ICB_Website.UI.Resources.RHome.Document, URL = "#" });
    ViewBag.Breadcrumb = list;
    var documentProvider = new ICB.Business.Access.DocumentProvider();
    var documents = documentProvider.VB_GetRecentPost().ToList();
    var documentFirst = documents.FirstOrDefault() == null ? new ICB.Business.Models.Document() : documents.FirstOrDefault();
    documents = documents.Count == 0 ? new List<ICB.Business.Models.Document>() : documents.Skip(1).ToList();
}
@section TopSlider{
    @Html.Partial("_sectionSlider")
}

@Html.Action("Vanban")

@Html.Action("tailieu")

